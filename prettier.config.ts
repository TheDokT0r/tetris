import type { Config } from "prettier";

const config: Config = {
  printWidth: 100,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  tabWidth: 2,
  arrowParens: "always",
};

export default config;
